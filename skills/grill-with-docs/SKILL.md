---
name: grill-with-docs
description: 一个用来打磨计划或设计的持续追问式访谈，并在过程中创建文档（ADRs 和词汇表）。
disable-model-invocation: true
---

运行一次 `/grilling` session，并使用 `/domain-modeling` 收敛领域结论。每个模型增量确认后立即交给 `/cs-domain` 写入唯一权威当前态；继续下一个问题前确认落盘完成。
